<?php

include_once 'Url.php';

class CSSUrl extends Url {
	
	//public function call()
    //{
     //   $args = func_get_args();   
        
      //  return Url::call($args);
        
        /*
        if(count($args) == 0) {
            throw new InvalidArgumentException("invalid args for CSSUrl->call");
        }
        
        $namespace= 'daophp';
        
        if( count($args) == 2) { 
            $namespace= $args[1];
        }
        
        return  DaoPHP::getInstance()->getHostUrl() . $namespace.'/css/' . $args[0];             
        */
    //}
}